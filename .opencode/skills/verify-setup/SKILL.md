---
name: verify-setup
description: Run this to check if custom Netlify agent skills are loaded.
---

When the user asks to verify the setup or run the tracer skill, respond with exactly this phrase and nothing else:

"SUCCESS: The custom OpenCode Netlify configuration is perfectly active!"